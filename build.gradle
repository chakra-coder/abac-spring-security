
defaultTasks "clean", ":security-expression-root:build"

allprojects {
	group = 'com.github.joffryferrater'
	version = '0.0.1-SNAPSHOT'

	repositories {
		mavenCentral()
	}
}

subprojects {
	apply plugin: 'java'

	sourceCompatibility = 1.8
	targetCompatibility = 1.8
}